#include "Saxpyobj.h"
/*
Saxpyobj::Saxpyobj(int val){
    int N = val;

    x = (float*) aligned_alloc(sizeof(float)*N,sizeof(float)*N);
    y = (float*) aligned_alloc(sizeof(float)*N,sizeof(float)*N);
}

void Saxpyobj::init(float val){ 

    a = val;
    int i;
    for (i=0; i<N; ++i){
        x[i]=i; 
        y[i]=i+2;
    }
}

void Saxpyobj::saxpy(){
//    saxpy_fpga(N, a, x, y);
}*/
