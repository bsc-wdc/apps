REDIS REQUIRES SOME DEPENDENCIES THAT NEED TO BE INSTALLED:

# Local
Redis
hiredis
python -m pip install redis-py-cluster --user

# MN4
module load ruby
wget http://rubygems.org/downloads/redis-3.3.0.gem
gem install -l redis-3.3.0.gem
gem list --check redis gem
# Python dependencies: ../redis
pip install hiredis-1.0.1.tar.gz --user
# The following packages may require to do CTRL+d after succesfully installed
pip install redis-3.0.1.tar.gz --user
pip install redis-py-cluster-2.0.0.tar.gz --user


TODO:
-----
WARNING: redis-trib.rb is not longer available!
You should use redis-cli instead.

All commands and features belonging to redis-trib.rb have been moved
to redis-cli.
In order to use them you should call redis-cli with the --cluster
option followed by the subcommand name, arguments and options.

Use the following syntax:
redis-cli --cluster SUBCOMMAND [ARGUMENTS] [OPTIONS]

Example:
redis-cli --cluster create 10.1.20.60:6379 10.1.20.61:6379 10.1.20.60:6380 --cluster-replicas 0

To get help about all subcommands, type:
redis-cli --cluster help
